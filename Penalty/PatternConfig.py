from Code.GeneralConfig.PatternConfig import *


penalty_pattern = sep.join([graph_name_str,
                            trunc_num_step_str,
                            penalty_str])

penalty_msg_pattern = sep.join([graph_name_str,
                                trunc_num_step_str,
                                penalty_str])
